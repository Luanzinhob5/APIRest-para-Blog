# Arquivo .gitignore para um projeto Flask robusto

# 1. Ambientes Virtuais
# Nunca comite o ambiente virtual, pois ele pode ser recriado a partir do requirements.txt
venv/
.venv/
env/
.env/
ENV/

# 2. Arquivos de Bytecode e Cache do Python
# Gerados pelo interpretador Python, não são código fonte.
__pycache__/
*.pyc
*.pyo
*.pyd

# 3. Bancos de Dados Locais
# O arquivo do banco de dados é um dado de tempo de execução, não parte do código.
# Cada desenvolvedor terá o seu. O que importa é o schema no models.py.
*.sqlite
*.sqlite3
*.db
blog.db

# 4. Credenciais e Segredos (MUITO IMPORTANTE)
# Para a fase avançada, guardaremos senhas de banco, chaves de API
# e segredos de JWT em um arquivo .env. ESTE ARQUIVO NUNCA PODE SER VERSIONADO.
.env
*.env

# 5. Arquivos de IDEs e Editores de Código
# Configurações específicas do ambiente de cada desenvolvedor.
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
.atom/

# 6. Arquivos de Teste e Cobertura (Fase Intermediária)
# Artefatos gerados ao rodar os testes.
.pytest_cache/
.coverage
htmlcov/
nosetests.xml
coverage.xml

# 7. Arquivos de Log e Dependências
# Logs devem ser inspecionados no servidor, não no Git.
*.log
logs/
pip-log.txt

# 8. Arquivos de Sistema Operacional
# Arquivos de metadados gerados pelo macOS, Windows e Linux.
.DS_Store
._*
Thumbs.db
desktop.ini
*~

# 9. Arquivos de Build e Distribuição
# Gerados ao empacotar a aplicação.
build/
dist/
*.egg-info/